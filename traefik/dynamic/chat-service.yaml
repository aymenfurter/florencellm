http:
  services:
    chat-service:
      loadBalancer:
        passHostHeader: false
        servers:
          - url: 'https://chat.internal.{{ env "CONTAINER_APP_ENV_DNS_SUFFIX" }}/conversation'
  routers:
    menuitem:
      rule: "Path(`/api/conversation`)"
      service: chat-service
